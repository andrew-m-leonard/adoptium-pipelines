{
  "stageId": "15-tck-tests",
  "stageDisabled": false,
  "stageTimeoutMinutes": 0,
  "stageCondition": [
    { "param": "RUN_TESTS",  "value": true },
    { "param": "ENABLE_TCK", "value": true }
  ],
  "description": "Parameters consumed by the TCK test stage.",
  "parameterGroups": [
    {
      "name": "Stage Selections",
      "description": "Boolean flags that control which pipeline stages are enabled. All stage-gate parameters are collected here so operators can see and adjust the full execution plan in one place.",
      "parameters": [
        {
          "name": "ENABLE_TCK",
          "type": "boolean",
          "default": false,
          "description": "Run TCK tests. Disabled by default — enable when TCK certification is required."
        }
      ]
    }
  ]
}
